# Layout Components Margin and Padding List

## src/components/mono-ab-test/style.css

**Selector:** `:host`
```css
margin: 2rem 0;
```

**Selector:** `.ab-test-wrapper`
```css
padding: 1.5rem;
```

**Selector:** `.ab-test-header`
```css
margin-top: 0;
margin-bottom: 1rem;
padding-bottom: 0.75rem;
padding-right: 4rem;
```

**Selector:** `.ab-test-error`
```css
padding: 0.75rem;
margin-bottom: 1rem;
```

**Selector:** `.controls-container`
```css
padding: 1rem;
margin-bottom: 1rem;
```

**Selector:** `.switch-group`
```css
padding: 0.25rem;
```

**Selector:** `.time-display`
```css
margin-left: 1rem;
```

**Selector:** `.progress-container`
```css
margin-bottom: 1.5rem;
```

**Selector:** `.evaluation-container`
```css
padding: 1.25rem;
```

**Selector:** `.eval-prompt`
```css
margin-bottom: 1rem;
```

**Selector:** `.btn-vote`
```css
padding: 0.5rem 1.5rem;
```

**Selector:** `.voted-message`
```css
margin-top: 1rem;
```

**Selector:** `.spinner`
```css
margin-bottom: 1rem;
```

## src/components/mono-account/style.css

**Selector:** `.mono-account-container`
```css
padding: 8px 12px;
```

**Selector:** `.mono-account-btn`
```css
padding: 4px 10px;
```

## src/components/mono-badge/style.css

**Selector:** `.mono-badge`
```css
padding-left: 0.563rem;
padding-right: 0.563rem;
```

## src/components/mono-code-block/style.css

**Selector:** `:host`
```css
margin: 1em 0;
```

**Selector:** `.code-block-header`
```css
padding: 0.5em 1em;
```

**Selector:** `.copy-button`
```css
padding: 0.4em 0.8em;
```

## src/components/mono-dice/style.css

**Selector:** `:host`
```css
margin: 1rem;
```

## src/components/mono-drawer/style.css

**Selector:** `.drawer-container.left .drawer-handle`
```css
padding: 10px 0;
```

**Selector:** `.drawer-container.right .drawer-handle`
```css
padding: 10px 0;
```

**Selector:** `.drawer-container.top .drawer-handle`
```css
padding: 0 10px;
```

**Selector:** `.drawer-container.bottom .drawer-handle`
```css
padding: 0 10px;
```

**Selector:** `.drawer-content`
```css
padding: 16px;
```

## src/components/mono-export/style.css

**Selector:** `.menu button`
```css
padding: 12px 16px;
```

## src/components/mono-flipcard/style.css

**Selector:** `mono-flipcard`
```css
margin: 4px;
```

**Selector:** `.flipcard-front, .flipcard-back`
```css
padding: 16px 24px;
```

## src/components/mono-group-assignment/style.css

**Selector:** `.mono-group-container`
```css
margin: 1.5em 0;
```

**Selector:** `.mono-group-alert`
```css
padding: 15px;
```

**Selector:** `.mono-group-title`
```css
margin-bottom: 10px;
```

**Selector:** `.mono-group-card`
```css
padding: 20px;
```

**Selector:** `.mono-group-label`
```css
margin-bottom: 5px;
```

## src/components/mono-hero/style.css

**Selector:** `:host`
```css
margin-top: calc(-1 * var(--body-padding-y, 5rem));
margin-bottom: 5rem;
```

**Selector:** `.hero-container`
```css
padding: 2rem;
```

**Selector:** `::slotted(h1),
.hero-content h1`
```css
margin: 0;
```

**Selector:** `::slotted(h2),
.hero-content h2`
```css
margin: 1rem 0;
```

**Selector:** `::slotted(p),
.hero-content p`
```css
margin: 1rem 0;
```

## src/components/mono-layout/style.css

**Selector:** `:host`
```css
margin: 1.5rem 0;
```

## src/components/mono-poll/style.css

**Selector:** `:host`
```css
margin: 1.5rem 0;
padding: 1.5rem;
```

**Selector:** `.poll-header`
```css
margin-top: 0;
margin-bottom: 1rem;
```

**Selector:** `.option-label`
```css
padding: 0.75rem 1rem;
```

**Selector:** `input[type="radio"]`
```css
margin-right: 1rem;
```

**Selector:** `button.submit-btn`
```css
margin-top: 1.5rem;
padding: 0.5rem 1.5rem;
```

**Selector:** `.voted-message`
```css
margin-top: 1rem;
```

## src/components/mono-reaction/style.css

**Selector:** `.mono-reaction-container`
```css
margin: 1em 0;
padding: 10px;
```

**Selector:** `.mono-reaction-btn`
```css
padding: 6px 12px;
```

**Selector:** `.mono-reaction-count`
```css
padding: 2px 6px;
```

## src/components/mono-section/style.css

**Selector:** `:host`
```css
margin-top: 2rem;
margin-bottom: 2rem;
```

**Selector:** `.section-container`
```css
padding: 3rem 2rem;
```

**Selector:** `.section-content`
```css
margin: 0 auto;
```

## src/components/mono-session-join/style.css

**Selector:** `.mono-session-join-container`
```css
margin: 1.5em 0;
padding: 15px;
```

**Selector:** `.mono-session-join-title`
```css
margin-bottom: 10px;
```

**Selector:** `.mono-session-input`
```css
padding: 8px 12px;
```

**Selector:** `.mono-session-btn`
```css
padding: 8px 16px;
```

**Selector:** `.mono-session-btn-secondary`
```css
padding: 6px 12px;
margin-top: 10px;
```

**Selector:** `.mono-session-error`
```css
margin-top: 5px;
```

**Selector:** `.mono-session-highlight`
```css
padding: 2px 6px;
```

## src/components/mono-sound/style.css

**Selector:** `.mono-sound-container`
```css
padding: 4px 8px;
margin: 4px;
```

**Selector:** `.mono-sound-play-btn`
```css
padding: 0;
margin: 0;
```

## src/components/mono-textfield-input/style.css

**Selector:** `.mono-textfield-container`
```css
margin: 1rem 0;
```

**Selector:** `.mono-textfield`
```css
padding: 0.5rem;
```

## src/components/mono-theme/style.css

**Selector:** `.theme-switcher-container`
```css
margin: 1rem 0;
padding: 1rem;
```

**Selector:** `select`
```css
padding: 0.25rem 0.5rem;
```

## src/templates/core/base.css

**Selector:** `*,
*::before,
*::after`
```css
margin: 0;
padding: 0;
```

**Selector:** `:root`
```css
--body-padding-y: 5rem;
```

**Selector:** `body`
```css
margin: 0;
padding: 0;
padding-block: var(--body-padding-y);
```

**Selector:** `hr`
```css
margin-top: 5rem;
margin-bottom: 5rem;
margin-left: auto;
margin-right: auto;
```

**Selector:** `h1`
```css
margin: 0 0 1.8rem;
```

**Selector:** `h2`
```css
margin: 3rem 0 1rem;
padding-bottom: 0.35rem;
```

**Selector:** `h3`
```css
margin: 2rem 0 0.6rem;
```

**Selector:** `p`
```css
margin: 0.6rem 0 1.3rem;
```

**Selector:** `p + p`
```css
margin-top: 0.5rem;
```

**Selector:** `a`
```css
padding-bottom: 0.05em;
```

**Selector:** `ul,
ol`
```css
margin: 0.4rem 0 1.4rem;
padding-left: 1.7rem;
```

**Selector:** `li + li`
```css
margin-top: 0.2rem;
```

**Selector:** `code`
```css
padding: 0.5rem;
```

**Selector:** `pre`
```css
margin: 1.5rem;
padding: 1.2rem;
```

**Selector:** `pre code`
```css
padding: 0;
```

**Selector:** `blockquote`
```css
margin-top: 1.5rem;
margin-bottom: 1.5rem;
padding-left: 1rem;
padding-right: 1rem;
padding-top: 0.1rem;
padding-bottom: 0.1rem;
```

**Selector:** `table`
```css
margin-top: 2rem;
margin-bottom: 4rem;
```

**Selector:** `th,
td`
```css
padding: 0.75rem;
```

**Selector:** `.colab-badge`
```css
margin-right: 6px;
```
